name: ai_insights_platform
description: AI Insights Platform - Flutter Web Frontend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  
  http: ^1.1.0
  provider: ^6.1.1

  
  file_picker: ^10.2.0

  
  cupertino_icons: ^1.0.2
  flutter_spinkit: ^5.2.0
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  web_socket_channel: ^3.0.3
  material_color_utilities: ^0.11.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # assets:
    # - assets/images/
    # - assets/icons/
